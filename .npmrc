# React 18 (admin) + React 19 (crm) no mesmo workspace exige flat node_modules,
# senao TSC ve @types/react duplicado via symlink paths e trava os JSX
# components de @base-ui/react. Trade-off: perde isolamento pnpm puro,
# mantem store global (ainda economiza disco).
node-linker=hoisted
